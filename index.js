let gameOrder = []; //keeps track of how lights flash
let userOrder = []; //keeps track of how user presses the lights
let flash; //tracks number of flashes
let turn; //tracks number of flashes
let good; //boolean for if user matched pattern or not
let compTurn; //boolean for if computer turn or user turn
